# NOMAD Web GUI - Ansible Inventory
# This file is auto-managed by the /deploy-gui skill

all:
  children:
    nomad_servers:
      hosts: {}
      # Hosts are added dynamically after provisioning
      # Example:
      # nomad-web-gui:
      #   ansible_host: 1.2.3.4
      #   ansible_user: root
