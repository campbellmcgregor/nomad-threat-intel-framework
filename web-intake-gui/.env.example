# NOMAD Web Intake GUI Configuration

# Required: API token for authenticating report submissions
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
NOMAD_WEB_API_TOKEN=

# Required: Secret key for cryptographic operations
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
NOMAD_SECRET_KEY=

# Optional: Default share link expiry in hours (default: 72)
SHARE_EXPIRY_HOURS=72

# Optional: Enable debug mode (default: false)
DEBUG=false

# Optional: CORS origins (comma-separated, default: *)
CORS_ORIGINS=*
